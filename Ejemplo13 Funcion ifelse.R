#Función if Else
a <- 10

ifelse(a>5, 5*a, 10*a)

ifelse(a<20, "Hola", "Adios")

ifelse(a>40, 1, 2)

x <- c(5,2,9,12)
ifelse(x>6, 4*x, 5*x) #Cada posición comparará el valor y por cada valor hará la condición que corresponda



