x<seq(20) #Creamos un vector desde una secuencia
x #Mostrarmos el vector x
any(x>50) #Validamos si el vector tiene algún valor mayor a 50
any(x>5) #Validamos si el vector tiene algún valor mayor a 5
y <- seq(5) #Creamos un vector con una secuencia de 5
y
all(y > 1) #Valida si todos los valores en el vector son mayor que 1
all(y>0) #Valida si todos los valores en el vector son mayor que 0
